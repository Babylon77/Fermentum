# Shared Models

This directory will contain shared serialized models and schema contracts.

It should define:

- core entities
- export DTOs
- sync payload shapes
- version markers

All changes here should be treated as compatibility-sensitive.

